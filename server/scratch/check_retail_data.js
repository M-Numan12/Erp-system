const { Pool } = require('pg');
// Let's query using the debug endpoint. We will add a temporary route to get summary counts of products, customers, sales, bank accounts grouped by module_type and user_id!
